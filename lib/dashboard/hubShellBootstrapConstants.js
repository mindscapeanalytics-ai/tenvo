/** Shared hub shell bootstrap page sizes (safe for client + server imports). */

/** Default lean inventory page size for hub shell bootstrap. */
export const HUB_SHELL_PRODUCT_PAGE_LIMIT = 100;
/** Bootstrap invoice page (no line items). */
export const HUB_SHELL_INVOICE_LIMIT = 200;
/** Recent Activity rows included in bootstrap. */
export const HUB_SHELL_ACTIVITY_LIMIT = 25;
