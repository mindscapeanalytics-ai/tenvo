export const metadata = {
  title: 'Marketing, CRM & growth | TENVO',
  description:
    'Campaigns, loyalty, CRM, quotations, and analytics in the same hub as inventory, POS, and finance — the growth layer teams expect from modern suites.',
};

export default function MarketingCrmLayout({ children }) {
  return children;
}
